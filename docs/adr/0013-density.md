# ADR 0013: Density and accessibility

**Decision:** Default intensity is logarithmic with a visible nonzero floor. Severity uses stable color plus row labels, outlines, and numeric tooltips.

**Alternatives:** Linear-only color encoding hides sparse data and excludes color-impaired users.

**Consequences and validation:** Theme/DPI golden rendering and contrast review are release checks.
