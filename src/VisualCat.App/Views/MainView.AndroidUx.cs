namespace VisualCat.App.Views;

// Compatibility stub intentionally left without MainView members.
// Android UX behavior is split by responsibility across MainView.Notice.cs,
// MainView.SafeArea.cs, and MainView.Workspace.cs so overlaying an updated source
// tree onto an older checkout cannot leave duplicate partial-class implementations.
