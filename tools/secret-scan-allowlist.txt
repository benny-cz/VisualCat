# Reviewed secret-scan findings that cannot be marked inline.
#
# Each non-comment line is a regular expression matched against a finding line
# produced by tools/scan-secrets.ps1, in the form:
#
#   <rule name> at <path>:<line> -> <redacted evidence>
#
# Only add an entry after classifying the finding, and record the reasoning
# above it. Findings in the working tree should use an inline `gitleaks:allow`
# marker instead, so the justification lives next to the code. This file exists
# for blobs in Git history, which can no longer be edited.
#
# Reviewed 2026-07-22: a test fixture literal asserting that diagnostic bundles
# redact sensitive values. Never a real credential. The current checkout carries
# an inline marker; these are the pre-marker blobs in history.
^Hard-coded password assignment at history:tests/VisualCat\.Application\.Tests/SessionPersistenceTests\.cs
